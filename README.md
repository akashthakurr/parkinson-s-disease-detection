# parkinson-s-disease-detection
Machine Learning model for Parkinson’s Disease prediction using Support Vector Machine (SVM). Implements data preprocessing, feature scaling, and model evaluation for reliable results.
